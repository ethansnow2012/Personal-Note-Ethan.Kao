
array_map(function($key, $value) {
    return [$key => $value];
}, array_keys($my_array), $my_array);



$result = array_combine(
     array_keys($arr), 
     array_map(function($v){ return $v; }, $arr)
);

function foo($blah, $x = null, $y = null) {
    if (null === $x) {
        $x = "some value";
    }

    if (null === $y) {
        $y = "some other value";
    }

    code here!

}


function ($var) use ($filterBy) {
